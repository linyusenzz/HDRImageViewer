<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="HdrImageViewer.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:HdrImageViewer.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="关于" />
        <StackPanel
            Grid.Row="1"
            MaxWidth="720"
            HorizontalAlignment="Left"
            Spacing="12">
            <TextBlock
                Style="{StaticResource SubtitleTextBlockStyle}"
                Text="HDR 图片查看器" />
            <TextBlock
                x:Name="VersionText"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="版本：读取中"
                TextWrapping="WrapWholeWords" />
            <TextBlock
                Text="基于 WinUI 3 和 DirectX HDR 渲染表面的图片查看器，支持 gain map 与单层 HDR 图片的独立解码和映射。"
                TextWrapping="WrapWholeWords" />
            <HyperlinkButton
                Content="微博主页"
                NavigateUri="https://weibo.com/u/6864619718"
                Padding="0" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="本产品包含 Adobe 授权的 Gain Map 技术。"
                TextWrapping="WrapWholeWords" />
        </StackPanel>
    </Grid>
</Page>
